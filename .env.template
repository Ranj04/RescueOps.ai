# EdgeOne Makers OpenAI-compatible model gateway — the ONLY model path (see llm_client.py)
LLM_BASE_URL=https://ai-gateway.edgeone.link/v1
MAKERS_MODELS_KEY=
LLM_PRIMARY_MODEL=@makers/deepseek-v4-flash
LLM_FALLBACK_MODEL=@makers/minimax-m2.7

# §6A on-call SMS channel (stretch B4a). SMS_ENABLED=false turns the whole
# channel off — the web approval panel is always the guaranteed path.
SMS_ENABLED=false
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=
ONCALL_PHONE_NUMBER=
# Exact public URL Twilio posts to — used to validate X-Twilio-Signature.
SMS_WEBHOOK_URL=
# Optional link appended to approval texts.
SITE_URL=
